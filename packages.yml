packages:
  - package: fishtown-analytics/dbt_utils
    version: 0.3.0
  
  - local: ~/Documents/github_repos/dbt_asana_source
  # The final version will use the following syntax, but this is a VERY last step :)
    # - package: fivetran/asana_source
    #   version: 0.0.1